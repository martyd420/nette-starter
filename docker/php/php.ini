memory_limit = 256M
upload_max_filesize = 32M
post_max_size = 32M
max_execution_time = 60
date.timezone = Europe/Prague

; Opcache stays in development-friendly mode here (timestamps validated);
; the production image overrides this with opcache-prod.ini.
opcache.enable = 1
opcache.enable_cli = 0
